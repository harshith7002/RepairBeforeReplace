import { DiagnosisHistoryView } from '@/components/DiagnosisHistoryView';

export const metadata = {
  title: 'Diagnosis Audit History — RepairBeforeReplace',
};

export default function HistoryPage() {
  return <DiagnosisHistoryView />;
}
