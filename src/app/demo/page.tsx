import { DemoFlow } from '@/components/DemoFlow';

export const metadata = {
  title: '90-Second Judge Demo Tour — RepairBeforeReplace',
};

export default function DemoPage() {
  return <DemoFlow />;
}
